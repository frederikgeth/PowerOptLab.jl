**/.DS_Store

# Code-coverage artifacts emitted by `julia --code-coverage` / Pkg.test(coverage=true)
*.cov
*.info
lcov.info

# Resolved dependency manifests — not consumed downstream for a library and a
# source of churn. The BMOPFTools source is pinned in Project.toml; use
# scripts/instantiate_pinned.jl to align local and CI environments.
Manifest.toml
**/Manifest.toml

# Documenter build output
docs/build/

# Scratch
/scratch/
