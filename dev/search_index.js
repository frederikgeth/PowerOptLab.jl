var documenterSearchIndex = {"docs":
[{"category":"section","location":"components/devices/#Storage-and-EV-devices","page":"Storage & EVs","text":"Kind: Component model · Maturity: promotion candidate · Direction: forward · Temporal: inter-temporal (state of charge)\n\nDevices are the reusable building blocks that solve_multiperiod_opf stamps into each network snapshot. A device is described by an SI-valued struct; the package handles the per-unit scaling, the current-injection stamping into the engine's KCL, and the inter-temporal state-of-charge linking.","title":"Storage & EV devices"},{"category":"section","location":"components/devices/#Model","page":"Storage & EVs","text":"Per snapshot, a device contributes a per-phase current injection (cr, ci) added to the engine's KCL accumulators, so it draws or supplies real physical current. Its aggregate AC injection is split into nonnegative charge and discharge power:\n\nP^textinj = p^textd - p^textc qquad p^textc p^textd ge 0\n\nwith pᶜ the power drawn to charge and pᵈ the power delivered when discharging (discharge is positive injection). The state of charge integrates these with one-way efficiencies across a period of length Delta t:\n\nE_t+1 = E_t + left(eta^textc p^textc_t - fracp^textd_teta^textdright)Delta t\nqquad E^min le E_t+1 le E^max\n\nRound-trip loss (eta  1) makes simultaneous charging and discharging suboptimal, so the split stays physical without an explicit complementarity constraint.","title":"Model"},{"category":"section","location":"components/devices/#Battery-/-storage","page":"Storage & EVs","text":"using PowerOptLab\nbat = StorageDevice(\n    id            = \"bat\",\n    bus           = \"bus1\",\n    p_charge_max  = 40e3,     # W\n    p_discharge_max = 40e3,   # W\n    energy_max    = 100e3,    # Wh\n    energy_init   = 40e3,     # Wh\n    eff_charge    = 0.95,\n    eff_discharge = 0.95,\n    cyclic        = true,     # terminal SOC == initial SOC\n)\n\nUse energy_final to pin the terminal energy to a specific value instead of the cyclic default, and q_min/q_max to allow reactive support (default is unity power factor). Multi-phase inverters are supported via phase_terminals; the neutral terminal is the return (nothing if referenced directly to ground).","title":"Battery / storage"},{"category":"section","location":"components/devices/#Electric-vehicles","page":"Storage & EVs","text":"An EVDevice is a storage device that is only controllable while plugged in and must reach a target energy by departure:\n\nev = EVDevice(\n    id               = \"ev1\",\n    bus              = \"bus1\",\n    p_charge_max     = 20e3,               # W  (V1G: no discharge)\n    energy_max       = 40e3,               # Wh\n    energy_init      = 10e3,               # Wh\n    available        = [true, true, true, false],  # unplugged in period 4\n    departure_energy = 30e3,               # Wh required …\n    departure_period = 3,                  # … by the end of period 3\n)\n\nWhile a period is unavailable the charger is idle and the state of charge is held. Set p_discharge_max > 0 for bidirectional (V2G) operation, letting the vehicle discharge into expensive periods subject to its departure requirement.\n\nSee the API reference for the full field list of StorageDevice and EVDevice.","title":"Electric vehicles"},{"category":"section","location":"contributing/#Contributing","page":"Contributing","text":"PowerOptLab is a staging ground: new work lands here first, and anything that matures folds back into the BMOPFTools spec. Before adding something, decide which of the three kinds of contribution it is — that determines where the code and docs go and which engine seam you reuse.","title":"Contributing"},{"category":"section","location":"contributing/#Adding-a-component-model","page":"Contributing","text":"A new network element. Put it in src/components/.\n\nDefine a struct describing the element (ports, ratings, physics parameters).\nStamp it into a solve with a model_hook!: add its per-phase current injection to the engine's KCL accumulators, plus any extra variables/constraints (e.g. a state-of-charge state).\nRead results back with a solution_hook!.\nAdd a page under docs/src/components/ with the badge line and a worked example; register it in docs/make.jl and export public names from PowerOptLab.jl.\n\nSee Storage & EVs as the reference example.","title":"Adding a component model"},{"category":"section","location":"contributing/#Adding-a-problem-specification","page":"Contributing","text":"A new formulation over the same physics. Put it in src/problems/.\n\nBuild the network snapshot(s) via the staged API — build_opf_model(add_objective=false), then compose your own objective and constraints, enforce_kcl! per snapshot, and extract_result.\nReturn a bespoke result struct.\nAdd a page under docs/src/problems/ (badge line + worked example), register in docs/make.jl, export from PowerOptLab.jl.\n\nSee State estimation for the inverse-problem pattern and Multi-period OPF for inter-temporal linking.","title":"Adding a problem specification"},{"category":"section","location":"contributing/#Adding-a-bespoke-algorithm","page":"Contributing","text":"A new solution method. Put it in src/algorithms/ and add a page under docs/src/algorithms/. See Bespoke algorithms.","title":"Adding a bespoke algorithm"},{"category":"section","location":"contributing/#Badge-line-convention","page":"Contributing","text":"Each capability page opens with a one-line metadata block so readers can place it at a glance:\n\n> **Kind:** Problem specification · **Maturity:** promotion candidate ·\n> **Direction:** inverse · **Temporal:** single-snapshot\n\nKind — Component model / Problem specification / Bespoke algorithm.\nMaturity — prototype (expect churn) or promotion candidate (ready to fold into the BMOPF spec).\nDirection — forward (dispatch/OPF) or inverse (estimation).\nTemporal — single-snapshot or inter-temporal.","title":"Badge line convention"},{"category":"section","location":"contributing/#The-promotion-path","page":"Contributing","text":"When a contribution stabilises — a settled interface, tests, and a worked example — it becomes a promotion candidate. Promotion means proposing the model or formulation for the BMOPF spec upstream, at which point the engine may absorb it and the PowerOptLab version becomes a thin re-export or is retired.","title":"The promotion path"},{"category":"section","location":"contributing/#Conventions","page":"Contributing","text":"SI at the interface; per-unit only inside a solve (via ctx.bases).\nManifest.toml is intentionally not committed (library convention).\nEvery feature is opt-in and covered by a test under test/.","title":"Conventions"},{"category":"section","location":"problems/parameter_estimation/#Parameter-estimation-(calibration)","page":"Parameter estimation","text":"Kind: Problem specification · Maturity: prototype · Direction: inverse · Temporal: inter-temporal (shared parameters)\n\nsolve_parameter_estimation calibrates uncertain network parameters — line lengths and transformer/regulator tap ratios — from smart-meter data. It is the shared-parameter dual of state estimation, and a multi-time-step extension of it:\n\n fixes estimates\nState estimation the parameters the per-snapshot state\nParameter estimation nothing about the uncertain elements the shared, time-invariant parameters\n\nThe formulation follows Vanin, Geth, Heidari & Van Hertem, Distribution System State and Impedance Estimation Augmented with Carson's Equations (arXiv:2506.04949): a weighted measurement-residual objective over a time series, the multiconductor IVR (rectangular current–voltage) power flow as the physics, time-invariant impedances written as nominal · length, and the smart-meter data as noisy (P, Q, |V|) triples per user.","title":"Parameter estimation (calibration)"},{"category":"section","location":"problems/parameter_estimation/#Why-multiple-time-steps","page":"Parameter estimation","text":"A single snapshot cannot separate a long line from a heavy load: both pull the downstream voltage down by the same amount. With T snapshots of diverse loads the shared parameters become over-determined — the one length (or tap) that explains every snapshot at once is pinned. Calibration therefore needs a time series; this is the essential difference from a one-shot state estimate.\n\nnote: Identifiability caveat\nAs Vanin et al. discuss, in realistic feeders (meters only at load buses, smart-meter noise) individual branch parameters generally cannot be recovered exactly — multiple equivalent solutions fit the data equally well. What is recoverable, and what matters for downstream power-flow/OPF, is the cumulative impedance along each path and a model that reproduces the measured voltages. The small radial examples here are deliberately well-conditioned so the estimates land close to the truth.","title":"Why multiple time steps"},{"category":"section","location":"problems/parameter_estimation/#Measurements","page":"Parameter estimation","text":"Each metered user contributes noisy (P, Q, |V|) readings as Measurements — the same struct as state estimation:\n\n:vmag — phase-to-neutral voltage magnitude [V].\n:pinj, :qinj — active/reactive power injected into the network [W]/[var] (negative for a load). A free injection current is added at each measured bus and fit to these; buses with no injection measurement are zero-injection.\n\nsigma is the standard deviation (WLS weight 1/σ²). Choose the objective with objective=:wls (smooth least squares) or objective=:wlav (weighted least absolute value — the robust, bad-data-rejecting choice used in Vanin et al.).","title":"Measurements"},{"category":"section","location":"problems/parameter_estimation/#How-the-uncertain-elements-are-stamped","page":"Parameter estimation","text":"The two element classes are handled differently, reflecting how the BMOPFTools engine exposes them:\n\nTransformer taps use the engine's native free-tap variable. A transformer carrying tap_min/tap_max gets a :tap decision variable that the engine threads through its per-unit-correct, base-referred winding constraints. CalibTap keeps the transformer in every snapshot net, sets those bounds, and the solver couples the tap equal across snapshots — the engine's transformer physics (leakage, losses, referral) is reused unchanged.\nLine lengths have no native free variable, so CalibLines are omitted from the physics nets and re-stamped here with a shared free length ℓ: V_f − V_t = (r₀+jx₀)·ℓ·I. This is the one genuine \"replace Ohm's law\" step; per-unit scaling by z_base is applied so SI and per-unit solves agree.","title":"How the uncertain elements are stamped"},{"category":"section","location":"problems/parameter_estimation/#Worked-example","page":"Parameter estimation","text":"using PowerOptLab\nusing BMOPFTools: parse_bmopf, solve_pf\n\n# Physics net for one snapshot: known source; the uncertain line l1 (src→b1) is\n# OMITTED (its length is the unknown); NO loads (injections come from the meters).\ncalnet() = parse_bmopf(\"\"\"\n{\"bus\":{\n   \"src\":{\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]},\n   \"b1\": {\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]}},\n \"voltage_source\":{\"vs\":{\"bus\":\"src\",\"terminal_map\":[\"1\"],\"v_magnitude\":[230.0],\"v_angle\":[0.0]}}}\n\"\"\"; from_string=true)\n\n# Per-snapshot meter readings (P, Q, |V|) at b1 — here for four diverse loads.\nmeas = [\n  [Measurement(kind=:vmag, bus=\"b1\", value=v,  sigma=0.3),\n   Measurement(kind=:pinj, bus=\"b1\", value=-p, sigma=50.0),   # negative = load\n   Measurement(kind=:qinj, bus=\"b1\", value=-q, sigma=50.0)]\n  for (v, p, q) in [(229.3, 1000.0, 200.0), (226.8, 4000.0, 800.0),\n                    (224.1, 7000.0, 1400.0), (228.0, 2500.0, 500.0)]\n]\nnets = [calnet() for _ in meas]\n\nr = solve_parameter_estimation(nets, meas;\n        lines=[CalibLine(id=\"l1\", bus_from=\"src\", bus_to=\"b1\",\n                         r_per_length=0.4, x_per_length=0.25)],\n        objective=:wls)      # or :wlav\n\nr.line_length[\"l1\"]   # estimated length of l1\nr.residual_rms        # RMS voltage misfit (V) — near the meter noise floor if good\nr.snapshots           # per-snapshot fitted state (SI)\n\nAdd CalibTaps (naming transformers present in the nets) to the taps keyword to calibrate taps jointly. Both per_unit=true and per_unit=false give identical estimates. A residual_rms well above the known meter noise is the diagnostic that something structural is still wrong (a misidentified tap, a missing line, an unmetered load).","title":"Worked example"},{"category":"section","location":"problems/parameter_estimation/#Scope","page":"Parameter estimation","text":"This is a prototype. It handles single-phase elements (one terminal per element end — add one CalibLine per phase for a multi-phase feeder). Estimating the series-impedance type via Carson's equations (conductor geometry/material) as in Vanin et al., regularising toward GIS priors, and four-wire mutual coupling are natural extensions left for later.\n\nSee the API reference for CalibLine, CalibTap, solve_parameter_estimation, and ParameterEstimationResult.","title":"Scope"},{"category":"section","location":"concepts/#Concepts","page":"Concepts","text":"PowerOptLab is a staging ground for experimental power-system modelling built on top of the BMOPFTools reference optimal-power-flow engine. BMOPFTools ships one small, correct four-wire rectangular current–voltage OPF and deliberately refuses to become a \"model zoo\"; PowerOptLab is where the zoo lives. Everything here reuses the engine's device physics, per-unit handling, and result extraction through its public extension seams — without forking the engine. Anything that matures into accepted practice can later be folded back into the BMOPF spec.","title":"Concepts"},{"category":"section","location":"concepts/#The-three-kinds-of-contribution","page":"Concepts","text":"Every capability in PowerOptLab is one of three structurally different things, distinguished by which layer of the engine it extends. The docs and the source tree (src/components/, src/problems/, src/algorithms/) are organised the same way.\n\nKind What it contributes Seam it reuses Source\nComponent model a new network element (device physics) model_hook! / solution_hook! src/components/\nProblem specification a new objective + variable/constraint structure over the whole network staged build_opf_model / enforce_kcl! / generation_cost / extract_result src/problems/\nBespoke algorithm a new solution method — how you solve, not what custom solve loops around the staged API src/algorithms/\n\nA component model answers \"what new thing can sit on the network?\" It is stamped into a snapshot as a per-phase current injection added to the engine's Kirchhoff-current-law accumulators, optionally with extra variables (e.g. a state-of-charge state linking snapshots).\nA problem specification answers \"what question do we ask of the same physics?\" It builds one or more snapshots into a JuMP model via the staged API, swaps in its own objective and constraints, and reads back a bespoke result. It changes the formulation, not the elements.\nA bespoke algorithm answers \"how do we solve it?\" — decomposition, sequential linearization, warm-start schemes, or any custom loop around the staged API. None exist yet; the slot is reserved.","title":"The three kinds of contribution"},{"category":"section","location":"concepts/#Cross-cutting-dimensions","page":"Concepts","text":"The three kinds above are the primary axis, but each capability also carries a few orthogonal attributes, surfaced as a badge line at the top of its page:\n\nMaturity — prototype (experimental, expect churn) vs promotion candidate (stable enough to fold back into the BMOPF spec). This is the whole point of a staging ground: knowing what is ready to graduate.\nDirection — forward (given parameters, find an operating point: dispatch/OPF) vs inverse (given measurements, find the state or parameters: state and parameter estimation).\nTemporal structure — single-snapshot vs inter-temporal (state-of-charge coupling, shared parameters, or per-interval envelopes spanning many snapshots).\n\nA future determinism axis (deterministic vs stochastic/robust) will slot in here when the first such contribution lands.","title":"Cross-cutting dimensions"},{"category":"section","location":"concepts/#Interface-conventions","page":"Concepts","text":"Everything is SI at the interface (watts, vars, watt-hours, volts); per-unit conditioning inside each solve is handled by the engine's ctx.bases. Where it applies, per_unit=true and per_unit=false give identical results.\n\nSee Contributing for how to add each kind of contribution and the path back to the BMOPF spec.","title":"Interface conventions"},{"category":"section","location":"components/advanced_inverter/#Advanced-inverter-(prototype)","page":"Advanced inverter","text":"Kind: Component model · Maturity: prototype · Direction: forward · Temporal: single-snapshot\n\nAdvancedInverter is a more detailed inverter-based-resource (IBR) than the BMOPFTools engine's built-in current-injection IBR. The engine models an IBR as a bounded current source at the point of connection (POC); this prototype adds the core structural idea from the BMOPFTools IBR model extensions design doc — an explicit internal AC node behind the converter — and the five feature phases layered on it.\n\n POC bus ──[filter r+jx (+ grid shunt b)]── internal node ──[converter]── DC\n   network sets V here                       EMF lives here          losses/ripple here\n\nOn top of that structure it carries exact three-phase feasible-region models from ongoing research — 3-leg (3-wire), 4-leg, and split-DC-link converters — whose DC-utilisation limit is the exact time-sampled switching-polytope condition with an endogenous 2ω bus-ripple derating and neutral-current limits (see Three-phase topology models below).\n\nIt is built entirely on the BMOPFTools staged API through a model_hook!; it does not modify the engine. Device parameters are SI; the solve runs in SI (per_unit=false) or per-unit (per_unit=true), scaling every parameter to model units via ctx.bases — the DC-side quantities (v_dc, c_dc, In_max) stay SI and the AC↔DC coupling scales through the POC bus's v_base/i_base/s_base. Results are returned in SI in both modes.","title":"Advanced inverter (prototype)"},{"category":"section","location":"components/advanced_inverter/#The-five-phases","page":"Advanced inverter","text":"Phase Feature Model\n0 Output filter (L/LC) series r+jx from POC to the internal node, optional grid-side shunt b\n1 Internal EMF / DC utilisation `\n2 Grid-forming balanced 120° internal EMF with a bounded magnitude decision variable\n3 Converter losses non-branching P_dc = P_ac + P_loss, `Ploss = plossfixed + aloss·\n4 Double-frequency ripple single-phase cap `\n\nEvery feature is opt-in: with only id, bus, and s_max the device is a plain grid-following converter, and the internal node collapses onto the POC when the filter is zero.","title":"The five phases"},{"category":"section","location":"components/advanced_inverter/#Key-modelling-choices-(from-the-design-doc)","page":"Advanced inverter","text":"Limits on the converter side. The apparent-power circle s_max and the current limit i_max are applied on the converter quantities (internal-node voltage × current), matching real nameplate — so an output filter reduces the power actually delivered to the grid below the converter rating.\nNon-branching losses. With AC power positive = injected to grid and DC power positive = drawn from the DC source, the single equation P_dc = P_ac + P_loss (P_loss ≥ 0) holds for both discharge and charge — no direction if-branch.\nGrid-forming ≠ slack. A grid-forming inverter holds a balanced, bounded internal EMF behind the filter, but does not replace the network's reference; the surrounding grid still needs a slack source.","title":"Key modelling choices (from the design doc)"},{"category":"section","location":"components/advanced_inverter/#Worked-example","page":"Advanced inverter","text":"using PowerOptLab\nusing BMOPFTools: parse_bmopf\n\n# A stiff grid: slack at \"grid\", short line to the inverter POC.\nnet = parse_bmopf(\"\"\"\n{\"bus\":{\n    \"grid\":{\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]},\n    \"poc\": {\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"],\"v_min\":[200.0],\"v_max\":[250.0]}},\n \"voltage_source\":{\"vs\":{\"bus\":\"grid\",\"terminal_map\":[\"1\"],\"v_magnitude\":[230.0],\"v_angle\":[0.0]}},\n \"linecode\":{\"lc\":{\"R_series_1_1\":0.05}},\n \"line\":{\"l1\":{\"bus_from\":\"grid\",\"bus_to\":\"poc\",\"terminal_map_from\":[\"1\"],\"terminal_map_to\":[\"1\"],\"linecode\":\"lc\",\"length\":1.0}}}\n\"\"\"; from_string=true)\n\n# A converter with an output filter and a three-term loss curve; minimise loss\n# while delivering 3 kW to the grid.\ninv = AdvancedInverter(id=\"inv\", bus=\"poc\", s_max=5000.0,\n                       r_filter=0.2, x_filter=0.5,\n                       p_loss_fixed=20.0, a_loss=0.3, c_loss=0.02)\n\nr = solve_advanced_inverter(net, inv; objective=:min_loss, p_set=3000.0)\n\nr.p_poc     # ≈ 3000 W delivered at the POC\nr.p_conv    # converter-side active power (> p_poc: filter losses)\nr.p_loss    # 20 + 0.3·|I| + 0.02·|I|²\nr.p_dc      # = p_conv + p_loss  (the non-branching DC-link balance)\nr.v_int_mag # internal EMF magnitude per phase (V)\n\nSwitch objective=:max_export to maximise POC active power and watch the converter rating, filter, EMF/modulation, or ripple limits bind. For a three-phase grid_forming=true inverter the solved internal EMF magnitudes are equal across phases (balanced 120°) and the 2ω ripple is ≈ 0.","title":"Worked example"},{"category":"section","location":"components/advanced_inverter/#Choosing-a-three-phase-topology","page":"Advanced inverter","text":"Set topology to one of :THREE_LEG, :FOUR_LEG, or :SPLIT_DC (with v_dc, c_dc, and In_max for the 4-wire ones) to use the exact switching-polytope model:\n\ninv = AdvancedInverter(id=\"inv\", bus=\"poc\", phase_terminals=[\"a\",\"b\",\"c\"], neutral=\"n\",\n                       topology=:FOUR_LEG, s_max=20e3, i_max=40.0,\n                       v_dc=700.0, c_dc=1.1e-3, In_max=40.0, m_max=0.96,\n                       r_filter=0.05, x_filter=0.15)\nr = solve_advanced_inverter(net3, inv)   # net3 = a three-phase grid\nr.i_neutral   # neutral current (A) — non-zero only under unbalance\nr.dv2         # 2ω bus-ripple amplitude (V) that derated the DC rails\n\nOn a balanced grid all three topologies coincide (no neutral current, no ripple). Under unbalance the 4-leg and split-DC draw neutral current (bounded by In_max), and the split-DC needs a higher v_dc for the same per-phase voltage — the half-bus utilisation penalty of the split-capacitor structure.\n\nSee the API reference for AdvancedInverter, solve_advanced_inverter, and InverterResult.","title":"Choosing a three-phase topology"},{"category":"section","location":"components/advanced_inverter/#Scope","page":"Advanced inverter","text":"This is a prototype for experimentation, not a validated engine feature. It implements the design doc's Phases 0–4 plus the three-phase topology models as a hook-stamped device; the reactive/active priority modes, sequence-current limits, and grid-forming-as-reference capabilities listed in the design doc's backlog are not included. If a piece of this matures, it can be folded back into the engine.","title":"Scope"},{"category":"section","location":"components/advanced_inverter/#three-phase-topology-models","page":"Advanced inverter","text":"For the three-phase topologies (:THREE_LEG, :FOUR_LEG, :SPLIT_DC) the crude scalar modulation cap is replaced by the exact time-sampled switching-polytope feasibility from ongoing research on converter feasible regions (fundamental-frequency RMS phasors; the classical pulse-width-modulation DC-utilisation and DC-link-ripple results of the power-electronics literature). All constraints apply to the converter output U_x = V_int_x (the internal node), so the filter, losses, grid-forming, and s_max circle all compose. The equations below are what the code stamps (shown in SI; per-unit is the same after base scaling).\n\nOscillating (2ω) power. The unconjugated phase sum\n\ntilde S = sum_xinabc U_x I_xqquad\ntilde S_re = textstylesum_x (U^re_x I^re_x - U^im_x I^im_x)\ntilde S_im = textstylesum_x (U^re_x I^im_x + U^im_x I^re_x)\n\nis the amplitude of the double-frequency power pulsation the DC-link capacitance absorbs (the two bilinear equalities are the only nonconvexity in the model).\n\nBus-ripple phasor. With DC capacitance C_eq (single cap C_eq = C_dc; split link's series caps give C_eq = C/2), the 2ω bus voltage ripple is a phasor D = j\\,\\tilde S/(2\\omega C_{eq} V_{dc}), i.e.\n\nD_re = -tilde S_im(2omega C_eq V_dc)qquad\nD_im =  tilde S_re(2omega C_eq V_dc)\n\ngiving the instantaneous DC rail v_{dc}(\\theta) = V_{dc} + D_{re}\\cos2\\theta - D_{im}\\sin2\\theta. An optional dv2_max caps \\sqrt{D_{re}^2+D_{im}^2}.\n\nSampled voltage feasibility. Over a uniform grid θ_k = 2π(k-1)/N (N = n_samples, default 36), both signs, with m = m_max:\n\n3-leg (3-wire) — pairwise line-to-line references must fit the bus, and no zero-sequence current flows:\n\npmsqrt2big(U^re_x-U^re_y)costheta_k - (U^im_x-U^im_y)sintheta_kbig\n   le mv_dc(theta_k)quad (xy)inabbccaqquad textstylesum_x I_x = 0\n\n4-leg — the fourth leg is a movable reference, so the pairwise conditions hold plus each phase against the neutral leg, and the neutral current is limited by the 4th-leg rating:\n\npmsqrt2bigU^re_xcostheta_k - U^im_xsintheta_kbig le mv_dc(theta_k)\nqquad I_n = Bigtextstylesum_x I_xBig le I_nmax\n\nsplit-DC (4-wire) — each phase is an independent half-bridge against the capacitor midpoint (half the bus), and the fundamental midpoint ripple N = I_{ret}/(j2\\omega C) (with I_{ret} = \\sum_x I_x) merges into the phase reference W_x = U_x + N:\n\npmsqrt2big(U^re_x+N_re)costheta_k - (U^im_x+N_im)sintheta_kbig\n   le tfracm2v_dc(theta_k)qquad I_n le I_nmax text(cap rating)\n\nwith N_{re} = -I^{im}_n/(2\\omega C), N_{im} = I^{re}_n/(2\\omega C). The factor of two on the rail is the split link's utilisation penalty: it needs roughly twice the DC voltage of the 4-leg for the same per-phase output.\n\nCurrent limits are per-phase |I_x| \\le i_{max} and the neutral limits above. The sampling makes these outer approximations, exact as N→∞ with relative gap ~(π/N)² (≈0.8 % at N=36). Every per-sample constraint is linear in the phasor variables and the ripple aux D; only the two \\tilde S equalities are nonlinear, so the model solves as a smooth NLP (Ipopt).","title":"Three-phase topology models"},{"category":"section","location":"problems/state_estimation/#State-estimation","page":"State estimation","text":"Kind: Problem specification · Maturity: promotion candidate · Direction: inverse · Temporal: single-snapshot\n\nsolve_state_estimation is a different problem specification over the same network physics: given noisy measurements of an energised network, find the bus voltage state that best fits them in a weighted-least-squares (WLS) sense.\n\nIt reuses the BMOPFTools device model but with three changes, all expressed through the public seams:\n\nNo operational bounds. Because build_opf_model adds limits only where the net declares them, a bounds-free net yields a pure physics model with free bus voltages.\nFree injections instead of fixed loads. A model_hook! adds a free injection current at each measured bus (so KCL closes with the voltages free to fit the data). Buses without an injection measurement are treated as zero-injection — the classic zero-injection pseudo-measurement.\nA residual objective instead of generation cost. The hook sets the WLS objective sum_i (z_i - h_i(textstate))^2  sigma_i^2 over the measurements.","title":"State estimation"},{"category":"section","location":"problems/state_estimation/#Measurements","page":"State estimation","text":"Each Measurement is an SI scalar with a standard deviation sigma (WLS weight 1sigma^2):\n\n:vmag — voltage magnitude at (bus, terminal) in volts.\n:pinj — active power injected into the network at (bus, terminal) in watts.\n:qinj — reactive power injection in vars.","title":"Measurements"},{"category":"section","location":"problems/state_estimation/#Worked-example","page":"State estimation","text":"using PowerOptLab\nusing BMOPFTools: parse_bmopf, solve_pf\n\n# A physics-only net: buses, lines, source; no operational limits.\nnet = parse_bmopf(\"\"\"\n{\"bus\":{\n    \"src\": {\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]},\n    \"bus1\":{\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]},\n    \"bus2\":{\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]}},\n \"voltage_source\":{\"vs\":{\"bus\":\"src\",\"terminal_map\":[\"1\"],\n     \"v_magnitude\":[1000.0],\"v_angle\":[0.0]}},\n \"linecode\":{\"lc\":{\"R_series_1_1\":0.5}},\n \"line\":{\n    \"l1\":{\"bus_from\":\"src\",\"bus_to\":\"bus1\",\"terminal_map_from\":[\"1\"],\"terminal_map_to\":[\"1\"],\"linecode\":\"lc\",\"length\":1.0},\n    \"l2\":{\"bus_from\":\"bus1\",\"bus_to\":\"bus2\",\"terminal_map_from\":[\"1\"],\"terminal_map_to\":[\"1\"],\"linecode\":\"lc\",\"length\":1.0}}}\n\"\"\"; from_string=true)\n\nmeas = [\n    Measurement(kind=:vmag, bus=\"bus1\", value=979.5,     sigma=2.0),\n    Measurement(kind=:pinj, bus=\"bus1\", value=-20_000.0, sigma=400.0),\n    Measurement(kind=:qinj, bus=\"bus1\", value=0.0,       sigma=400.0),\n    Measurement(kind=:vmag, bus=\"bus2\", value=969.2,     sigma=2.0),\n    Measurement(kind=:pinj, bus=\"bus2\", value=-20_000.0, sigma=400.0),\n    Measurement(kind=:qinj, bus=\"bus2\", value=0.0,       sigma=400.0),\n]\n\nse = solve_state_estimation(net, meas)\n\nse.bus[\"bus1\"][\"1\"][\"vm\"]   # estimated |V| at bus1 (V)\nse.residuals                # per-measurement measured/estimated/residual/normalized\nse.objective                # optimal weighted-residual sum\n\nWith more measurements than unknowns (here six measurements for four voltage components), the fused estimate filters measurement noise: it is typically closer to the true state than the raw voltage readings, because it reconciles every measurement through the network physics.\n\nSee the API reference for Measurement, solve_state_estimation, and StateEstimationResult.","title":"Worked example"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"All exported names, grouped by capability.","title":"API reference"},{"category":"section","location":"api/#Module","page":"API reference","text":"","title":"Module"},{"category":"section","location":"api/#Devices","page":"API reference","text":"","title":"Devices"},{"category":"section","location":"api/#Multi-period-OPF","page":"API reference","text":"","title":"Multi-period OPF"},{"category":"section","location":"api/#State-estimation","page":"API reference","text":"","title":"State estimation"},{"category":"section","location":"api/#Parameter-estimation","page":"API reference","text":"","title":"Parameter estimation"},{"category":"section","location":"api/#Dynamic-operating-envelopes","page":"API reference","text":"","title":"Dynamic operating envelopes"},{"category":"section","location":"api/#Advanced-inverter","page":"API reference","text":"","title":"Advanced inverter"},{"category":"section","location":"api/#Index","page":"API reference","text":"","title":"Index"},{"category":"module","location":"api/#PowerOptLab","page":"API reference","text":"PowerOptLab\n\nA staging ground for component models and problem specifications built on top of the BMOPFTools reference OPF engine, using its public extension seams (model_hook! / solution_hook! and the staged build_opf_model / enforce_kcl! / generation_cost / extract_result API) rather than forking the engine.\n\nThe BMOPFTools engine deliberately stays small and correct — it ships one four-wire rectangular current–voltage OPF and refuses to be a \"model zoo\". PowerOptLab is where the zoo lives: experimental devices (storage, EVs) and alternative problem specifications (multi-period co-optimisation, weighted least-squares state estimation) that reuse the engine's device physics, per-unit handling, and result extraction unchanged. Anything here that matures into accepted practice can later be folded back into the spec and the engine.\n\nContributions are organised by what layer of the engine they extend:\n\nComponent models — new network elements (src/components/)\n\nStamped into a solve through model_hook! / solution_hook!.\n\nStorage / EV (StorageDevice, EVDevice) — battery/EV inverter ports stamped as current injections with an inter-temporal state-of-charge state.\nAdvanced inverter (AdvancedInverter) — a prototype internal-AC-node IBR with an output filter, internal-EMF/DC-modulation bounds, grid-forming operation, converter losses, and double-frequency ripple limits.\n\nProblem specifications — new formulations over the staged API (src/problems/)\n\nA different objective/variable/constraint structure on the same physics.\n\nMulti-period OPF (solve_multiperiod_opf) — several network snapshots co-optimised in one model with storage/EV state linking each step to the next.\nState estimation (solve_state_estimation) — weighted least-squares estimation of the network state from noisy measurements (an inverse problem on the same physics).\nParameter estimation (solve_parameter_estimation) — calibration of uncertain line lengths and transformer tap ratios from smart-meter data across multiple time steps (the shared-parameter dual of state estimation).\nDynamic operating envelopes (solve_operating_envelope) — per-connection-point DER export limits that respect the network's voltage and thermal constraints, recomputed per interval.\n\nBespoke algorithms — new solution methods (src/algorithms/)\n\nCustom solve loops around the staged API (decomposition, sequential linearization, warm-start schemes). None yet — the slot is reserved.\n\nEverything is SI at the interface; per-unit conditioning inside the solve is handled via the engine's ctx.bases.\n\n\n\n\n\n","title":"PowerOptLab"},{"category":"type","location":"api/#PowerOptLab.StorageDevice","page":"API reference","text":"StorageDevice(; id, bus, kwargs...)\n\nA grid-connected battery (or generic energy-storage) inverter with an inter-temporal state of charge. Powers are SI watts, energies SI watt-hours.\n\nRequired\n\nid::String — unique device id.\nbus::String — connection bus.\np_charge_max, p_discharge_max — charge / discharge power limits (W ≥ 0).\nenergy_max — usable energy capacity (Wh).\nenergy_init — energy at the start of the horizon (Wh).\n\nOptional\n\nphase_terminals=[\"1\"], neutral=\"n\" — the inverter's phase conductor(s) and the return terminal (nothing if referenced directly to ground).\nenergy_min=0.0 — lower energy bound (Wh).\nq_min=0.0, q_max=0.0 — reactive-power box (var); default is unity power factor.\neff_charge=1.0, eff_discharge=1.0 — one-way efficiencies (0,1].\ncyclic=true — require the terminal state of charge to equal energy_init.\nenergy_final=nothing — if set, pin the terminal energy to this value (Wh), overriding cyclic.\n\n\n\n\n\n","title":"PowerOptLab.StorageDevice"},{"category":"type","location":"api/#PowerOptLab.EVDevice","page":"API reference","text":"EVDevice(; id, bus, available, departure_energy, kwargs...)\n\nAn electric-vehicle charger: a storage device that is only controllable while plugged in and must reach a target energy by departure. Set p_discharge_max > 0 for bidirectional (V2G) charging; the default 0.0 gives unidirectional (V1G).\n\nRequired\n\nid, bus — as StorageDevice.\np_charge_max — charge power limit (W).\nenergy_max, energy_init — battery capacity and plug-in energy (Wh).\navailable::Vector{Bool} — per period, whether the vehicle is plugged in. While unavailable the charger is idle and the state of charge is held.\ndeparture_energy — energy (Wh) required by departure_period.\n\nOptional\n\np_discharge_max=0.0 — V2G discharge limit (W); 0.0 ⇒ charge-only.\ndeparture_period=nothing — period index by whose end departure_energy must be met; nothing ⇒ the end of the horizon.\nphase_terminals, neutral, energy_min, q_min, q_max, eff_charge, eff_discharge — as StorageDevice.\n\n\n\n\n\n","title":"PowerOptLab.EVDevice"},{"category":"function","location":"api/#PowerOptLab.solve_multiperiod_opf","page":"API reference","text":"solve_multiperiod_opf(nets, devices; kwargs...) -> MultiperiodResult\n\nCo-optimise a sequence of network snapshots nets (one BMOPFTools net dict per period, in chronological order) with a set of storage/EV devices whose state of charge couples the periods. The snapshots share one JuMP model and one objective (the sum of each snapshot's generation cost); the devices arbitrage across time subject to their power, energy, efficiency, and terminal/departure constraints.\n\nPer-period economics come from the snapshots themselves — e.g. a time-varying slack import price set via each net's voltage_source cost, or differing loads.\n\nArguments\n\nnets::Vector — T network dicts (parse_bmopf output), one per period.\ndevices::Vector — StorageDevice / EVDevice instances. A device's bus/terminals must exist in every snapshot.\n\nKeywords\n\ndt_h=1.0 — period duration in hours (SOC integrates power over this).\nper_unit=true, s_base=1e6 — engine unit handling (results are SI regardless).\noptimizer=Ipopt.Optimizer, verbose=false, solver_options=() — solver control.\n\nReturns\n\nA MultiperiodResult with the per-period solutions and each device's SI charge/discharge/SOC trajectory.\n\n\n\n\n\n","title":"PowerOptLab.solve_multiperiod_opf"},{"category":"type","location":"api/#PowerOptLab.MultiperiodResult","page":"API reference","text":"MultiperiodResult\n\nResult of solve_multiperiod_opf.\n\nFields\n\ntermination_status::String — JuMP status of the single shared solve.\nobjective::Float64 — optimal objective value.\nsnapshots::Vector{Dict{String,Any}} — the per-period BMOPFTools result dict (SI), one per input net, in order.\ndispatch::Dict{String,NamedTuple} — per device id, SI trajectories over the horizon: p_charge, p_discharge, p_net (discharge positive), q (each length T), and soc (length T+1, energy in Wh at each step boundary, soc[1] = initial).\n\n\n\n\n\n","title":"PowerOptLab.MultiperiodResult"},{"category":"type","location":"api/#PowerOptLab.Measurement","page":"API reference","text":"Measurement(; kind, bus, value, sigma, terminal=\"1\")\n\nA single scalar measurement for solve_state_estimation. value and sigma are SI (volts for :vmag, watts for :pinj, vars for :qinj); sigma is the measurement standard deviation (WLS weight 1/sigma²).\n\nkind::Symbol — :vmag (voltage magnitude at (bus, terminal)), :pinj (active power injected into the network at (bus, terminal)), or :qinj (reactive power injection).\nbus::String, terminal::String=\"1\" — where the quantity is measured (terminal is the phase conductor; injections are referenced to neutral, see solve_state_estimation).\n\n\n\n\n\n","title":"PowerOptLab.Measurement"},{"category":"function","location":"api/#PowerOptLab.solve_state_estimation","page":"API reference","text":"solve_state_estimation(net, measurements; kwargs...) -> StateEstimationResult\n\nEstimate the network state of net (a physics-only BMOPFTools net: buses, lines, transformers, and a voltage source, ideally carrying no operational limits) that best fits measurements in a weighted-least-squares sense.\n\nKeywords\n\nneutral=\"n\" — return terminal for injection measurements and free injections. Pass nothing if phase terminals are referenced directly to ground.\nper_unit=true, s_base=1e6 — engine unit handling; measurements stay SI.\noptimizer=Ipopt.Optimizer, verbose=false, solver_options=().\n\nReturns\n\nA StateEstimationResult with the estimated SI bus voltages and the per-measurement residuals.\n\n\n\n\n\n","title":"PowerOptLab.solve_state_estimation"},{"category":"type","location":"api/#PowerOptLab.StateEstimationResult","page":"API reference","text":"StateEstimationResult\n\nResult of solve_state_estimation.\n\nFields\n\ntermination_status::String, objective::Float64 — solver status and the optimal weighted-residual sum ∑ (z−h)²/σ².\nbus::Dict{String,Any} — the estimated SI bus voltages (vr, vi, vm, va per terminal), i.e. the BMOPFTools result[\"bus\"].\nresiduals::Vector{NamedTuple} — per input measurement, in order: (kind, bus, terminal, measured, estimated, residual, normalized) with residual = measured − estimated and normalized = residual/σ (SI).\n\n\n\n\n\n","title":"PowerOptLab.StateEstimationResult"},{"category":"type","location":"api/#PowerOptLab.CalibLine","page":"API reference","text":"CalibLine(; id, bus_from, bus_to, r_per_length, x_per_length, ...)\n\nAn uncertain line whose length is a free parameter estimated by solve_parameter_estimation. The series impedance is (r_per_length + j·x_per_length)·length [Ω]; only length is estimated (the nominal per-length impedance is treated as known, following the construction-code model Z = Zⁿᵒᵐ·ℓ of Vanin et al.).\n\nThe uncertain lines must be omitted from the physics nets — they are the unknowns, stamped by this function. Single-phase: it connects (bus_from, terminal) to (bus_to, terminal); add one CalibLine per phase for a multi-phase line.\n\nKeywords\n\nid::String — label for reporting.\nbus_from::String, bus_to::String, terminal::String=\"1\" — the endpoints.\nr_per_length::Float64, x_per_length::Float64 — per-unit-length series R/X [Ω].\nlength_init::Float64=1.0 — starting guess handed to the solver.\nlength_min::Float64=0.1, length_max::Float64=10.0 — bounds on the estimate.\n\n\n\n\n\n","title":"PowerOptLab.CalibLine"},{"category":"type","location":"api/#PowerOptLab.CalibTap","page":"API reference","text":"CalibTap(; id, tap_min=0.9, tap_max=1.1)\n\nAn uncertain transformer/regulator tap estimated by solve_parameter_estimation. Unlike CalibLine, the transformer stays in the physics nets: this just names the transformer id and the tap bounds. The function sets tap_min/tap_max on that transformer so the engine creates its native free-tap variable, then couples the tap equal across all snapshots and reports the estimate.\n\nThe reported estimate is the tap multiplier τ on the nominal ratio N₀ = v_nom_from / v_nom_to (so τ = 1 is nominal); the effective turns ratio is N = N₀·τ.\n\nKeywords\n\nid::String — transformer id present in every snapshot net.\ntap_min::Float64=0.9, tap_max::Float64=1.1 — bounds on the multiplier τ.\n\n\n\n\n\n","title":"PowerOptLab.CalibTap"},{"category":"function","location":"api/#PowerOptLab.solve_parameter_estimation","page":"API reference","text":"solve_parameter_estimation(nets, measurements; lines, taps, kwargs...)\n    -> ParameterEstimationResult\n\nCalibrate uncertain line lengths and transformer tap ratios from smart-meter time series. The uncertain elements' parameters are shared, time-invariant unknowns; each snapshot supplies noisy (P, Q, |V|) meter readings that are fit in a weighted-least-squares (or robust weighted-least-absolute-value) sense across the whole horizon.\n\nThe meters' loads are not baked in as exact injections — each measured user gets a free injection current fit to its noisy P/Q readings (as in state estimation), and the phase-to-neutral voltage magnitude is fit to |V|. Buses with no injection measurement are zero-injection.\n\nArguments\n\nnets::AbstractVector — T per-snapshot physics nets (parse_bmopf output): the known source, the known lines, and the transformers named by taps (kept, so the engine frees their taps). The uncertain lines are omitted — they are the unknowns. No load objects are needed; injections come from the measurements.\nmeasurements::AbstractVector — parallel to nets; measurements[t] is a Vector{Measurement} of that snapshot's meter readings. :vmag (SI volts), :pinj, :qinj (SI W/var, injection into the network — negative for a load) are all treated as noisy, weighted by their sigma.\n\nKeywords\n\nlines::AbstractVector=CalibLine[], taps::AbstractVector=CalibTap[] — the uncertain elements to estimate (at least one required).\nneutral=\"n\" — return terminal for the phase-to-neutral projections; pass nothing if phase terminals are referenced directly to ground.\nobjective=:wls — :wls (weighted least squares, smooth) or :wlav (weighted least absolute value, better bad-data rejection; the choice in Vanin et al.).\nper_unit=true, s_base=1e6 — engine unit handling; measurements stay SI.\noptimizer=Ipopt.Optimizer, verbose=false, solver_options=().\n\nReturns\n\nA ParameterEstimationResult with the estimated lengths and tap multipliers, the RMS voltage residual, and the per-snapshot fitted state.\n\n\n\n\n\n","title":"PowerOptLab.solve_parameter_estimation"},{"category":"type","location":"api/#PowerOptLab.ParameterEstimationResult","page":"API reference","text":"ParameterEstimationResult\n\nResult of solve_parameter_estimation.\n\nFields\n\ntermination_status::String, objective::Float64 — solver status and the optimal weighted-residual value.\nline_length::Dict{String,Float64} — estimated length per CalibLine id.\ntap::Dict{String,Float64} — estimated tap multiplier τ per CalibTap id.\nresidual_rms::Float64 — root-mean-square voltage-measurement residual [V], a quick goodness-of-fit / noise-floor check.\nsnapshots::Vector{Dict{String,Any}} — per-snapshot extract_result (SI), the fitted network state at the estimated parameters.\n\n\n\n\n\n","title":"PowerOptLab.ParameterEstimationResult"},{"category":"type","location":"api/#PowerOptLab.ConnectionPoint","page":"API reference","text":"ConnectionPoint(; id, bus, export_max, kwargs...)\n\nA DER connection point whose export limit a solve_operating_envelope allocation computes. export_max is the inverter/connection active-power ceiling (W); the allocated envelope never exceeds it and is usually smaller, bound by network voltage/thermal constraints.\n\nid::String, bus::String — identifier and connection bus.\nexport_max::Float64 — maximum exportable active power (W ≥ 0).\nphase_terminals=[\"1\"], neutral=\"n\" — phase conductor(s) and return terminal (nothing if referenced directly to ground). Export is at unity power factor.\n\n\n\n\n\n","title":"PowerOptLab.ConnectionPoint"},{"category":"function","location":"api/#PowerOptLab.solve_operating_envelope","page":"API reference","text":"solve_operating_envelope(nets, connection_points; fairness=:equal, kwargs...)\n    -> OperatingEnvelopeResult\n\nCompute a dynamic operating envelope: for each interval (each net in nets), allocate an active-power export limit to every connection point such that all points can export simultaneously without violating the network's operational constraints (the voltage/thermal bounds the net declares).\n\nEach interval is an independent constrained-allocation solve on BMOPFTools.solve_opf: a model_hook! stamps a free export at each connection point and sets the fairness objective, so the engine's voltage and thermal limits bound the result.\n\nArguments\n\nnets::Vector — one BMOPFTools net dict per interval, carrying that interval's baseline loads and its operational limits (v_min/v_max, line i_max, …).\nconnection_points::Vector{ConnectionPoint} — the DERs to allocate to.\n\nKeywords\n\nfairness=:equal — allocation rule:\n:equal maximises a common export level assigned to every point (equitable; the level is capped by the weakest point and the tightest constraint);\n:sum maximises the total allocated export (efficient; points with more network headroom get more, and a weak point may receive ≈0);\n:proportional maximises sum(log(pₑ)) over the points (proportional / Nash–Kelly fairness) — a middle ground where no point is starved but a point with more headroom still gets more.\nper_unit=true, s_base=1e6, optimizer=Ipopt.Optimizer, verbose=false, solver_options=() — solver control, as in solve_opf.\n\nReturns\n\nAn OperatingEnvelopeResult with each connection point's allocated export (W) per interval.\n\nReactive power is held at zero (unity-PF export); import envelopes and power-factor-flexible allocation are natural extensions.\n\n\n\n\n\n","title":"PowerOptLab.solve_operating_envelope"},{"category":"type","location":"api/#PowerOptLab.OperatingEnvelopeResult","page":"API reference","text":"OperatingEnvelopeResult\n\nResult of solve_operating_envelope. All powers SI (W).\n\nFields\n\ntermination_status::Vector{String} — solver status per interval.\nenvelope::Dict{String,Vector{Float64}} — per connection-point id, the allocated export limit (W) for each interval.\ntotal_export::Vector{Float64} — sum of allocated exports across points, per interval.\nsnapshots::Vector{Dict{String,Any}} — the per-interval BMOPFTools result dict (bus voltages at the allocation, etc.).\n\n\n\n\n\n","title":"PowerOptLab.OperatingEnvelopeResult"},{"category":"type","location":"api/#PowerOptLab.AdvancedInverter","page":"API reference","text":"AdvancedInverter(; id, bus, s_max, kwargs...)\n\nA prototype inverter with an internal AC node behind an output filter. All parameters are SI. Features are opt-in: with only id, bus, s_max (and a zero filter) it is a plain grid-following converter at the POC.\n\nRequired\n\nid::String, bus::String — identifier and point-of-connection bus.\ns_max::Float64 — converter apparent-power rating (VA), applied on the converter side (internal-node voltage × current).\n\nConnection\n\nphase_terminals=[\"1\"], neutral=\"n\" — phase conductor(s) and return terminal (nothing ⇒ referenced to ground). The three-phase topologies and grid-forming require three phases.\n\nTopology (three-phase switching-polytope DC-utilisation model)\n\ntopology=:SINGLE_PHASE — one of :SINGLE_PHASE (internal-EMF model), :THREE_LEG, :FOUR_LEG, :SPLIT_DC. The three-phase topologies apply the exact time-sampled switching-polytope voltage feasibility on the internal-node voltage and require v_dc and c_dc (and In_max for the 4-wire ones).\nv_dc — DC-link voltage (V). c_dc — DC-link capacitance (F; per half for split).\nm_max=1.0 — modulation index limit (dimensionless).\nIn_max — neutral current limit (A): split = cap ripple rating, 4-leg = leg rating.\ndv2_max — optional cap on the 2ω bus-ripple amplitude (V).\nn_samples=36 — time-sampling grid for the exact feasibility (gap ~(π/N)²).\nf=50.0 — fundamental frequency (Hz).\n\nOutput filter\n\nr_filter=0.0, x_filter=0.0 — series filter impedance per phase (Ω).\nb_filter_shunt=0.0 — grid-side (POC) shunt susceptance (S).\n\nInternal EMF box / single-phase modulation\n\nv_int_min, v_int_max — per-phase EMF magnitude box (V; applies to all topologies).\nmodulation_max — SINGLEPHASE only: DC-link cap `|Vint| ≤ modulationmax·vdc/√3`.\n\nGrid-forming\n\ngrid_forming=false — balanced positive-sequence internal EMF; the magnitude v_gfm ∈ [v_int_min, v_int_max] is a decision variable (composes with a topology).\n\nConverter losses\n\np_loss_fixed=0.0 (W), a_loss=0.0 (W/A), c_loss=0.0 (W/A²).\n\nDouble-frequency ripple / current\n\np_ripple_max — SINGLE_PHASE only: bound on the 2ω power-ripple magnitude (VA).\ni_max=nothing — optional per-conductor current limit (A).\n\n\n\n\n\n","title":"PowerOptLab.AdvancedInverter"},{"category":"function","location":"api/#PowerOptLab.solve_advanced_inverter","page":"API reference","text":"solve_advanced_inverter(net, inverter; objective=:max_export, kwargs...)\n    -> InverterResult\n\nStamp inverter into net and solve, demonstrating the prototype internal-node model. The network supplies the surrounding grid (a voltage source, lines, any loads); the inverter injects at its POC bus.\n\nObjective\n\n:max_export — maximise the active power delivered to the grid at the POC.\n:min_loss — minimise converter loss subject to p_set active-power delivery.\n\nKeywords\n\np_set=nothing — required active-power target (W) for :min_loss.\nq_set=nothing — optional reactive-power constraint at the POC (var).\nper_unit=false, s_base=1e6, optimizer=Ipopt.Optimizer, verbose=false, solver_options=(). Per-unit results are returned in SI regardless.\n\n\n\n\n\n","title":"PowerOptLab.solve_advanced_inverter"},{"category":"type","location":"api/#PowerOptLab.InverterResult","page":"API reference","text":"InverterResult\n\nResult of solve_advanced_inverter. Powers SI (W / var / VA), voltages V, currents A.\n\nFields\n\ntermination_status::String, topology::Symbol\np_poc, q_poc — active/reactive power injected at the POC (grid side).\np_conv, q_conv — converter-side power (at the internal node).\np_loss, p_dc — converter loss and DC-link power (p_dc = p_conv + p_loss).\nv_int_mag::Vector{Float64} — internal EMF magnitude per phase.\ni_mag::Vector{Float64} — converter current magnitude per phase.\ni_neutral — neutral current magnitude |I_n| (0 for 3-wire / single-phase).\nripple — 2ω power-ripple magnitude |Σ V_int·I| (VA).\ndv2 — 2ω bus-ripple amplitude (V; three-phase topologies, else 0).\nbus::Dict{String,Any} — the BMOPFTools result[\"bus\"] (POC voltages, …).\n\n\n\n\n\n","title":"PowerOptLab.InverterResult"},{"category":"section","location":"algorithms/#Bespoke-algorithms","page":"Bespoke algorithms","text":"This section is a reserved slot — there are no bespoke algorithms yet.\n\nA bespoke algorithm is a new solution method: it changes how a problem is solved rather than what is solved. Where a problem specification hands one monolithic model to Ipopt, an algorithm wraps the staged API in a custom loop. Candidates include:\n\nDecomposition — spatial (per-feeder) or temporal (per-snapshot) splitting with a coordinating master problem.\nSequential linearization / SLP-SQP — solve a sequence of tractable approximations to convergence.\nADMM / operator splitting — distributed consensus across sub-networks.\nWarm-start schemes — reuse one solve's state to accelerate the next (e.g. across the snapshots of a multi-period run).\n\nWhen the first one lands, add it under src/algorithms/ and give it a page here. See Contributing for the pattern.","title":"Bespoke algorithms"},{"category":"section","location":"problems/operating_envelope/#Dynamic-operating-envelopes","page":"Operating envelopes","text":"Kind: Problem specification · Maturity: promotion candidate · Direction: forward · Temporal: per-interval\n\nA dynamic operating envelope (DOE) is the time-varying active-power export limit a network operator allocates to each connection point so that, when every point exports at its allocated limit at once, the network's operational limits (voltage bounds, thermal ratings) still hold. solve_operating_envelope computes one.\n\nUnlike state estimation, a DOE keeps the engine's operational bounds — they are exactly what limit the envelope. Each interval is an independent constrained allocation solved on solve_opf with a model_hook! that stamps a free export at each connection point and replaces the generation-cost objective with a fairness objective; a solution_hook! reads the allocation back out. \"Dynamic\" refers to recomputing per interval as the baseline load (and thus the available headroom) changes; the intervals are otherwise independent.","title":"Dynamic operating envelopes"},{"category":"section","location":"problems/operating_envelope/#Fairness-rules","page":"Operating envelopes","text":"Three allocation rules trade equity against efficiency:\n\n:equal maximises a single export level assigned to every point. The result is equitable but capped by the weakest point and the tightest constraint.\n:sum maximises the total allocated export. The result is efficient but can be uneven — an electrically stronger point (nearer the source, more headroom) may receive most of the allocation, and a weak point may get ≈0.\n:proportional maximises sum_i log(p^e_i) (proportional / Nash–Kelly fairness). This is the middle ground: no point is starved (the log drives every allocation strictly positive), yet a point with more network headroom still receives more. Its total sits between the :equal and :sum extremes.","title":"Fairness rules"},{"category":"section","location":"problems/operating_envelope/#Worked-example","page":"Operating envelopes","text":"An LV feeder with DER at two buses. As baseline load rises the local consumption absorbs export and frees voltage headroom, so the envelope grows — the dynamic response a static export limit misses.\n\nusing PowerOptLab\nusing BMOPFTools: parse_bmopf\n\nfeeder(p1, p2) = parse_bmopf(\"\"\"\n{\"bus\":{\n    \"src\": {\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]},\n    \"bus1\":{\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"],\"v_min\":[216.0],\"v_max\":[245.0]},\n    \"bus2\":{\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"],\"v_min\":[216.0],\"v_max\":[245.0]}},\n \"voltage_source\":{\"vs\":{\"bus\":\"src\",\"terminal_map\":[\"1\"],\"v_magnitude\":[230.0],\"v_angle\":[0.0]}},\n \"linecode\":{\"lc\":{\"R_series_1_1\":0.4}},\n \"line\":{\n    \"l1\":{\"bus_from\":\"src\",\"bus_to\":\"bus1\",\"terminal_map_from\":[\"1\"],\"terminal_map_to\":[\"1\"],\"linecode\":\"lc\",\"length\":1.0},\n    \"l2\":{\"bus_from\":\"bus1\",\"bus_to\":\"bus2\",\"terminal_map_from\":[\"1\"],\"terminal_map_to\":[\"1\"],\"linecode\":\"lc\",\"length\":1.0}},\n \"load\":{\n    \"d1\":{\"bus\":\"bus1\",\"terminal_map\":[\"1\",\"n\"],\"configuration\":\"SINGLE_PHASE\",\"p_nom\":[$p1],\"q_nom\":[0.0]},\n    \"d2\":{\"bus\":\"bus2\",\"terminal_map\":[\"1\",\"n\"],\"configuration\":\"SINGLE_PHASE\",\"p_nom\":[$p2],\"q_nom\":[0.0]}}}\n\"\"\"; from_string=true)\n\ncps  = [ConnectionPoint(id=\"der1\", bus=\"bus1\", export_max=10e3),\n        ConnectionPoint(id=\"der2\", bus=\"bus2\", export_max=10e3)]\nnets = [feeder(200.0, 200.0),      # low load  → tight envelope\n        feeder(5000.0, 5000.0)]    # high load → more headroom\n\nenv = solve_operating_envelope(nets, cps; fairness=:equal)\n\nenv.envelope[\"der1\"]   # e.g. [≈3.2e3, ≈8.0e3]  — grows with load (W)\nenv.total_export       # sum across connection points, per interval (W)\n\nWith :equal both points receive the same limit and the far bus sits at its v_max; fairness=:sum raises the total but skews it toward the stronger point, possibly starving the weaker one; and fairness=:proportional keeps every point above zero while still rewarding headroom — the efficiency/equity tradeoff a DOE policy must choose between. For example, at low baseline load the weak point's allocation is roughly 0 under :sum, its :equal share under :equal, and an in-between positive value under :proportional.\n\nSee the API reference for ConnectionPoint, solve_operating_envelope, and OperatingEnvelopeResult.\n\nReactive power is held at zero (unity-PF export); import envelopes and power-factor-flexible allocation are natural extensions.","title":"Worked example"},{"category":"section","location":"#PowerOptLab.jl","page":"Home","text":"A staging ground for component models, problem specifications, and bespoke algorithms built on top of the BMOPFTools reference optimal-power-flow engine.\n\nBMOPFTools ships one small, correct four-wire rectangular current–voltage OPF and deliberately refuses to become a \"model zoo\". PowerOptLab is where the zoo lives: experimental devices and alternative problem formulations that reuse the engine's device physics, per-unit handling, and result extraction through its public extension seams — without forking the engine. Anything here that matures into accepted practice can later be folded back into the BMOPF spec.\n\nNew here? Start with Concepts for the three kinds of contribution and the engine seams they reuse, then browse the capabilities below.","title":"PowerOptLab.jl"},{"category":"section","location":"#Capabilities","page":"Home","text":"","title":"Capabilities"},{"category":"section","location":"#Component-models","page":"Home","text":"New network elements, stamped via model_hook! / solution_hook!.\n\nCapability Entry point Maturity\nStorage / battery with state of charge StorageDevice promotion candidate\nEV charging (V1G / V2G) with availability & departure energy EVDevice promotion candidate\nAdvanced inverter (internal-node IBR) AdvancedInverter prototype","title":"Component models"},{"category":"section","location":"#Problem-specifications","page":"Home","text":"New formulations over the same physics, via the staged build_opf_model / enforce_kcl! / generation_cost / extract_result API.\n\nCapability Entry point Direction Maturity\nMulti-period OPF co-optimising many snapshots solve_multiperiod_opf forward promotion candidate\nState estimation (weighted least squares) solve_state_estimation inverse promotion candidate\nParameter estimation (line lengths / taps) solve_parameter_estimation inverse prototype\nDynamic operating envelopes (DER export limits) solve_operating_envelope forward promotion candidate","title":"Problem specifications"},{"category":"section","location":"#Bespoke-algorithms","page":"Home","text":"New solution methods (custom solve loops). None yet — see Bespoke algorithms for the reserved slot.","title":"Bespoke algorithms"},{"category":"section","location":"#Installation","page":"Home","text":"BMOPFTools is not yet registered, so develop both from local checkouts (or Git URLs):\n\nusing Pkg\nPkg.activate(\".\")\nPkg.develop(path=\"../BMOPFTools.jl\")   # or Pkg.develop(url=\"https://github.com/frederikgeth/BMOPFTools.jl\")\nPkg.instantiate()\n\nEverything is SI at the interface (watts, vars, watt-hours, volts); per-unit conditioning inside each solve is handled by the engine's ctx.bases.","title":"Installation"},{"category":"section","location":"#How-it-fits-together","page":"Home","text":"See Concepts for how each capability layers over the BMOPFTools staged API, and Contributing for how to add your own and the promotion path back to the BMOPF spec. Each capability page carries a worked example.","title":"How it fits together"},{"category":"section","location":"problems/multiperiod/#Multi-period-OPF","page":"Multi-period OPF","text":"Kind: Problem specification · Maturity: promotion candidate · Direction: forward · Temporal: inter-temporal\n\nsolve_multiperiod_opf co-optimises a sequence of network snapshots in one JuMP model, with storage/EV devices whose state of charge links each period to the next. This is the formulation the single-snapshot solve_opf cannot express, because state of charge couples time steps.\n\nUnder the hood it uses the BMOPFTools staged API: every snapshot is built into one shared model with build_opf_model(add_objective=false), each device's state of charge is linked across the snapshots, the per-snapshot generation costs are summed into one objective, KCL is enforced per snapshot, and the model is solved once.","title":"Multi-period OPF"},{"category":"section","location":"problems/multiperiod/#Worked-example:-battery-arbitrage","page":"Multi-period OPF","text":"Two periods with a time-varying slack import price (set via each snapshot's voltage_source cost). A cyclic battery discharges into the expensive period and recharges in the cheap one:\n\nusing PowerOptLab\nusing BMOPFTools: parse_bmopf\n\nnet(price) = parse_bmopf(\"\"\"\n{\"bus\":{\n    \"sourcebus\":{\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"]},\n    \"bus1\":     {\"terminal_names\":[\"1\",\"n\"],\"perfectly_grounded_terminals\":[\"n\"],\n                 \"v_min\":[900.0],\"v_max\":[1100.0]}},\n \"voltage_source\":{\"vs\":{\"bus\":\"sourcebus\",\"terminal_map\":[\"1\"],\n     \"v_magnitude\":[1000.0],\"v_angle\":[0.0],\"cost\":[$price]}},\n \"linecode\":{\"lc\":{\"R_series_1_1\":0.1}},\n \"line\":{\"l1\":{\"bus_from\":\"sourcebus\",\"bus_to\":\"bus1\",\n     \"terminal_map_from\":[\"1\"],\"terminal_map_to\":[\"1\"],\"linecode\":\"lc\",\"length\":1.0}},\n \"load\":{\"ld1\":{\"bus\":\"bus1\",\"terminal_map\":[\"1\",\"n\"],\n     \"configuration\":\"SINGLE_PHASE\",\"p_nom\":[100000.0],\"q_nom\":[0.0]}}}\n\"\"\"; from_string=true)\n\nnets = [net(0.20), net(0.05)]      # expensive, then cheap\nbat  = StorageDevice(id=\"bat\", bus=\"bus1\",\n                     p_charge_max=40e3, p_discharge_max=40e3,\n                     energy_max=100e3, energy_init=40e3, cyclic=true)\n\nres = solve_multiperiod_opf(nets, [bat]; dt_h=1.0)\n\nres.dispatch[\"bat\"].p_net   # ≈ [ +40e3, −40e3 ]  discharge then charge (W)\nres.dispatch[\"bat\"].soc     # ≈ [ 40e3, 0.0, 40e3 ]  (Wh, cyclic)","title":"Worked example: battery arbitrage"},{"category":"section","location":"problems/multiperiod/#Multiple-devices","page":"Multi-period OPF","text":"Pass any mix of StorageDevice and EVDevice; each gets its own state-of-charge trajectory and constraints, all co-optimised in the same model:\n\nres = solve_multiperiod_opf(nets, [bat, ev1, ev2]; dt_h=0.5)\n\nPer-period economics come from the snapshots themselves — a time-varying import price, differing loads, or any other per-snapshot data the BMOPFTools objective sees.","title":"Multiple devices"},{"category":"section","location":"problems/multiperiod/#Result","page":"Multi-period OPF","text":"solve_multiperiod_opf returns a MultiperiodResult: the per-period BMOPFTools result dicts (res.snapshots) plus each device's SI p_charge, p_discharge, p_net, q, and soc trajectories in res.dispatch. See the API reference for solve_multiperiod_opf and MultiperiodResult.","title":"Result"}]
}
